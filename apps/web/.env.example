# Identity and sessions (required before production use)
AUTH_SECRET=
DATABASE_URL=
DATABASE_SSL=true
ADMIN_INITIAL_EMAIL=bernie@r3alm.com
ADMIN_INITIAL_PASSWORD=
ENABLE_DEMO_LOGIN=false

# Optional legacy single-password fallback
APP_ACCESS_PASSWORD=

# OpenAI (server-only)
OPENAI_API_KEY=
OPENAI_MODEL=

# Incoming mail (server-only)
IMAP_HOST=
IMAP_PORT=993
IMAP_SECURE=true
IMAP_USER=
IMAP_PASSWORD=
MAIL_ARCHIVE_FOLDER=Archive

# Outgoing mail (server-only)
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
